import 'package:flutter/material.dart';

// tela de responsabilidade do joaquim
class CotacaoPage extends StatelessWidget {
  const CotacaoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('cotação')),
      body: const Center(
        child: Text('tela de login vazia'),
      ),
    );
  }
}