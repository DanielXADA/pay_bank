import 'package:flutter/material.dart';

// tela de responsabilidade do joaquim
class TransferenciaPage extends StatelessWidget {
  const TransferenciaPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('transferência')),
      body: const Center(
        child: Text('tela de transferência vazia'),
      ),
    );
  }
}